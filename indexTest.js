const name = "Joe";  // Change to match the expected value
const height = 34;   // Ensure height is less than 40
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
console.log(message);
